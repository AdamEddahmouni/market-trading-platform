# RT-01 Workflows

## WF-RT01-001 — RT-01 baseline evidence

- **Objective:** Produce measured baseline and export trace evidence.
- **Entry:** `baseline` (`RT01.OP.BASELINE`)
- **Then:** `export` (`RT01.OP.EXPORT`)
- **Terminal:** `SUCCESS` when baseline report and export complete.

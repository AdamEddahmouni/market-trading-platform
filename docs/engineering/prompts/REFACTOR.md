# Prompt: Refactor

Refactor `{area}` with no behavior change.

1. Confirm test coverage exists before editing
2. Small incremental commits/steps
3. Run `validate.py changed` + targeted tests after each step
4. No scope creep — no feature additions
5. WORK_LOG if structural/doc impact

# RT-01 Agent Operating Rules

Tracing must not change domain output. Never fabricate spans for absent stages.
Never label fixture replay as live provider evidence. Never treat measured
baseline as an SLA. Never store secrets in span attributes.
